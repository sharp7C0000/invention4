<% include header %>

<core-header-panel flex>

  <% var activeTab = 1; %>
  <% include admin_topbar %>

  <div ng-controller="AdminSetting">

    <div id="contents">

      <div class="page-title">
        <core-icon icon="settings"></core-icon>&nbsp;Blog Setting
      </div>

      <form name="loginForm">

        <paper-input-decorator
          label="Blog Title"
          floatingLabel
          error="{{errorMessage(loginForm.username.$error)}}"
          isInvalid="{{!loginForm.username.$valid && loginForm.username.$dirty}}"
        >
          <input name="username" is="paper-input"
            required
            ng-model="formData.username"
            ng-maxlength="10"
            ng-minlength="4"
            ng-keydown="hideGlobalValidation()"
          >
        </paper-input-decorator>

        <paper-dropdown-menu label="Post per page" style="width:100%">
        <paper-dropdown class="dropdown">
          <core-menu class="menu">
            <paper-item>1</paper-item>
            <paper-item>3</paper-item>
            <paper-item>5</paper-item>
            <paper-item>10</paper-item>
            <paper-item>20</paper-item>
          </core-menu>
        </paper-dropdown>
      </paper-dropdown-menu>

      <h3>Profile</h3>

      <div>
        <img holder="holder.js/125x125/text:PHOTO"></img>
        <paper-button class="primary" raised>CHANGE</paper-button>
        <paper-button class="primary" raised>REMOVE</paper-button>
      </div>

  
    <div>

      <textarea>
        Some text
      </textarea>
    </div>

      </form>

    </div>

  </div>

  </div>
</core-header-panel>

<script src="/components/requirejs/require.js" data-main="/js/admin_setting.js"></script>

<% include footer %>
